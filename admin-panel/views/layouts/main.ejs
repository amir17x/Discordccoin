<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | پنل مدیریت Ccoin</title>
  
  <!-- فونت‌های وازیر و فونت‌آوسم -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vazirmatn@33.0.3/Vazirmatn-font-face.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
  
  <!-- بوت‌استرپ و استایل‌های اصلی -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <link rel="stylesheet" href="/admin/assets/css/style.css">
  
  <!-- اسکریپت‌های ضروری -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.3.0/dist/chart.umd.min.js" defer></script>
  <script src="/admin/assets/js/main.js" defer></script>
  
  <!-- اسکریپت‌های اضافی برای صفحه داشبورد -->
  <% if (active === 'dashboard') { %>
    <script src="/admin/assets/js/dashboard.js" defer></script>
  <% } %>
</head>
<body>
  <div class="wrapper">
    <!-- سایدبار -->
    <%- include('../partials/sidebar') %>
    
    <div class="main-content">
      <!-- هدر -->
      <%- include('../partials/header') %>
      
      <!-- محتوای اصلی -->
      <div class="container-fluid py-3">
        <!-- پیام‌های فلش -->
        <% if (messages && (messages.success.length || messages.error.length || messages.info.length)) { %>
          <div class="flash-messages mb-4">
            <% if (messages.success.length) { %>
              <div class="alert alert-success d-flex align-items-center alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i>
                <div><%= messages.success %></div>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
            <% } %>
            
            <% if (messages.error.length) { %>
              <div class="alert alert-danger d-flex align-items-center alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i>
                <div><%= messages.error %></div>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
            <% } %>
            
            <% if (messages.info.length) { %>
              <div class="alert alert-info d-flex align-items-center alert-dismissible fade show" role="alert">
                <i class="fas fa-info-circle me-2"></i>
                <div><%= messages.info %></div>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
            <% } %>
          </div>
        <% } %>
        
        <!-- محتوای صفحه -->
        <%- body %>
      </div>
      
      <!-- فوتر -->
      <footer class="footer">
        <div class="container-fluid">
          <div class="row">
            <div class="col-md-6">
              <span class="text-muted">پنل مدیریت Ccoin &copy; <%= new Date().getFullYear() %></span>
            </div>
            <div class="col-md-6 text-end">
              <span class="text-muted">نسخه 1.0.0</span>
            </div>
          </div>
        </div>
      </footer>
    </div>
  </div>
</body>
</html>