<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>خطا | پنل ادمین ربات Ccoin</title>
    
    <!-- فونت آیکون Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <!-- فونت‌های فارسی -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v33.003/Vazirmatn-font-face.css">
    
    <!-- استایل Bootstrap RTL -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css">
    
    <style>
        body {
            font-family: 'Vazirmatn', sans-serif;
            background-color: #13151f;
            color: #e9ecef;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }
        
        .error-container {
            background-color: #1c1f2e;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
            z-index: 1;
            text-align: center;
        }
        
        .error-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            color: #f04747;
        }
        
        .error-code {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1rem;
        }
        
        .error-message {
            margin-bottom: 2rem;
        }
        
        .error-details {
            background-color: #272b3f;
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 2rem;
            text-align: left;
            max-height: 200px;
            overflow-y: auto;
            font-family: monospace;
            font-size: 0.9rem;
        }
        
        .btn-primary {
            background-color: #7289da;
            border-color: #7289da;
        }
        
        .btn-primary:hover {
            background-color: #5e73bc;
            border-color: #5e73bc;
        }
        
        .background-shape {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.15;
            z-index: 0;
        }
        
        .shape-1 {
            width: 600px;
            height: 600px;
            background-color: #f04747;
            top: -300px;
            left: -300px;
        }
        
        .shape-2 {
            width: 500px;
            height: 500px;
            background-color: #7289da;
            bottom: -250px;
            right: -250px;
        }
    </style>
</head>
<body>
    <!-- اشکال پس‌زمینه -->
    <div class="background-shape shape-1"></div>
    <div class="background-shape shape-2"></div>
    
    <div class="error-container">
        <div class="error-icon">
            <i class="bi bi-exclamation-triangle-fill"></i>
        </div>
        <div class="error-code">
            <%= error.status || 500 %>
        </div>
        <h2 class="error-message">
            <%= message || 'خطای سرور' %>
        </h2>
        
        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
        <div class="error-details">
            <%= error.stack %>
        </div>
        <% } %>
        
        <div class="d-flex justify-content-center">
            <a href="/" class="btn btn-primary mx-2">
                <i class="bi bi-house-door me-2"></i> بازگشت به خانه
            </a>
            <a href="javascript:history.back()" class="btn btn-outline-secondary mx-2">
                <i class="bi bi-arrow-left me-2"></i> بازگشت
            </a>
        </div>
    </div>
    
    <!-- اسکریپت‌های Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>