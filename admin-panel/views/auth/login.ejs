<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ورود به پنل مدیریت | Ccoin</title>
  
  <!-- فونت‌های وازیر و بوت‌استرپ آیکون‌ها -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vazirmatn@33.0.3/Vazirmatn-font-face.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  
  <!-- بوت‌استرپ و استایل‌های اصلی -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <link rel="stylesheet" href="/css/vision-ui-utilities.css">
  
  <style>
    body {
      font-family: 'Vazirmatn', sans-serif;
      background: var(--vui-body-bg);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow-x: hidden;
      position: relative;
      color: var(--vui-text-color);
    }
    
    /* انیمیشن پس‌زمینه */
    .animated-background {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      z-index: -1;
      overflow: hidden;
    }
    
    .background-blur {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(112.83deg, rgba(4, 8, 15, 0.3) 0%, rgba(21, 25, 40, 0.6) 110.84%);
      backdrop-filter: blur(10px);
      z-index: 0;
    }
    
    .circle {
      position: absolute;
      border-radius: 50%;
      filter: blur(40px);
    }
    
    .circle-1 {
      width: 450px;
      height: 450px;
      bottom: -100px;
      right: -100px;
      background: var(--vui-secondary-gradient);
      opacity: 0.4;
      animation: float 15s ease-in-out infinite;
    }
    
    .circle-2 {
      width: 550px;
      height: 550px;
      top: -150px;
      left: -100px;
      background: var(--vui-primary-gradient);
      opacity: 0.4;
      animation: float 18s ease-in-out infinite alternate;
    }
    
    .circle-3 {
      width: 300px;
      height: 300px;
      top: 40%;
      left: 50%;
      transform: translateX(-50%);
      background: var(--vui-info-gradient);
      opacity: 0.2;
      animation: pulse 8s ease-in-out infinite;
    }
    
    /* انیمیشن‌ها */
    @keyframes float {
      0% {
        transform: translateY(0px) scale(1);
      }
      50% {
        transform: translateY(-40px) scale(1.05);
      }
      100% {
        transform: translateY(0px) scale(1);
      }
    }
    
    @keyframes pulse {
      0% {
        transform: translateX(-50%) scale(1);
        opacity: 0.2;
      }
      50% {
        transform: translateX(-50%) scale(1.3);
        opacity: 0.3;
      }
      100% {
        transform: translateX(-50%) scale(1);
        opacity: 0.2;
      }
    }
    
    .login-container {
      width: 100%;
      max-width: 450px;
      padding: 20px;
      z-index: 10;
      position: relative;
    }
    
    .login-card {
      background: var(--vui-card-bg);
      border-radius: 20px;
      box-shadow: var(--vui-card-shadow);
      overflow: hidden;
      border: 1px solid var(--vui-border-color);
      transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
      backdrop-filter: blur(15px);
    }
    
    .login-card:hover {
      transform: translateY(-10px);
    }
    
    .login-header {
      background: var(--vui-primary-gradient);
      color: white;
      padding: 30px 20px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .login-header:before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url("data:image/svg+xml,%3Csvg width='20' height='20' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 19L19 1M1 1L19 19' stroke='rgba(255,255,255,0.1)' stroke-width='2'/%3E%3C/svg%3E") repeat;
      opacity: 0.2;
      z-index: 0;
    }
    
    .login-header .logo-container {
      width: 90px;
      height: 90px;
      margin: 0 auto 15px;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(5px);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      z-index: 1;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
    }
    
    .login-header .logo {
      width: 60px;
      height: 60px;
      object-fit: contain;
    }
    
    .login-header h3 {
      margin-bottom: 10px;
      font-weight: 700;
      position: relative;
      z-index: 1;
    }
    
    .login-header p {
      opacity: 0.8;
      margin-bottom: 0;
      position: relative;
      z-index: 1;
    }
    
    .login-body {
      padding: 30px;
    }
    
    .form-group {
      margin-bottom: 25px;
      position: relative;
    }
    
    .form-label {
      display: block;
      margin-bottom: 10px;
      font-weight: 500;
      color: var(--vui-text-muted);
    }
    
    .form-control {
      background: var(--vui-input-bg);
      border: 1px solid var(--vui-border-color);
      border-radius: 15px;
      color: var(--vui-text-color);
      padding: 12px 20px;
      height: auto;
      font-size: 1rem;
      transition: all 0.3s;
      width: 100%;
    }
    
    .form-control:focus {
      border-color: var(--vui-primary-color);
      background: var(--vui-input-focus-bg);
      box-shadow: 0 0 0 3px rgba(114, 76, 255, 0.2);
    }
    
    .input-icon {
      position: absolute;
      right: 15px;
      top: 45px;
      color: var(--vui-text-muted);
    }
    
    .btn-login {
      background: var(--vui-primary-gradient);
      color: white;
      border: none;
      border-radius: 15px;
      padding: 14px;
      font-weight: 600;
      width: 100%;
      position: relative;
      overflow: hidden;
      transition: all 0.3s;
      box-shadow: 0 5px 15px rgba(114, 76, 255, 0.3);
    }
    
    .btn-login:before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: all 0.6s;
    }
    
    .btn-login:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(114, 76, 255, 0.4);
    }
    
    .btn-login:hover:before {
      left: 100%;
    }
    
    .btn-login:active {
      transform: translateY(0);
    }
    
    .message-toast {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 1000;
      min-width: 300px;
      background: rgba(220, 53, 69, 0.9);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: white;
      border-radius: 10px;
      text-align: center;
      box-shadow: 0 5px 25px rgba(0, 0, 0, 0.2);
    }
    
    .year-text {
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.7);
      margin-top: 15px;
      position: relative;
      z-index: 1;
    }
  </style>
</head>

<body>
  <!-- انیمیشن پس‌زمینه -->
  <div class="animated-background">
    <div class="background-blur"></div>
    <div class="circle circle-1"></div>
    <div class="circle circle-2"></div>
    <div class="circle circle-3"></div>
  </div>

  <!-- پیغام‌های فلش -->
  <% if (messages && messages.error && messages.error.length > 0) { %>
    <div class="alert message-toast fade show" role="alert">
      <i class="bi bi-exclamation-triangle-fill me-2"></i>
      <%= messages.error %>
      <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% } %>

  <div class="login-container">
    <div class="login-card floating-card">
      <div class="login-header">
        <div class="logo-container">
          <img src="https://i.imgur.com/K9wtXo6.png" alt="Ccoin Logo" class="logo">
        </div>
        <h3>پنل مدیریت Ccoin</h3>
        <p>لطفاً برای دسترسی به پنل مدیریت وارد شوید</p>
        <div class="year-text">
          <%= new Date().getFullYear() %> © Ccoin
        </div>
      </div>
      
      <div class="login-body">
        <form action="/admin/login" method="POST">
          <div class="form-group">
            <label for="username" class="form-label">نام کاربری</label>
            <i class="bi bi-person input-icon"></i>
            <input type="text" class="form-control" id="username" name="username" placeholder="نام کاربری خود را وارد کنید" required>
          </div>
          
          <div class="form-group">
            <label for="password" class="form-label">رمز عبور</label>
            <i class="bi bi-lock input-icon"></i>
            <input type="password" class="form-control" id="password" name="password" placeholder="رمز عبور خود را وارد کنید" required>
          </div>
          
          <button type="submit" class="btn-login mt-4">
            <i class="bi bi-box-arrow-in-left me-2"></i>
            ورود به پنل مدیریت
          </button>
        </form>
      </div>
    </div>
  </div>

  <!-- اسکریپت‌های ضروری -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>