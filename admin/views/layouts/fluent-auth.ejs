<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | پنل مدیریت CCOIN</title>
    
    <!-- فونت‌ها -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- آیکون‌ها -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- استایل‌های فلوئنت UI -->
    <link rel="stylesheet" href="/admin/public/css/fluent.css">
    
    <!-- اسکریپت آیکون‌های feather -->
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
    
    <!-- اسکریپت‌های اضافی -->
    <% if (typeof extraStyles !== 'undefined') { %>
        <%- extraStyles %>
    <% } %>
</head>
<body>
    <div class="fluent-login-page">
        <div class="fluent-login-wrapper">
            <!-- پیام‌های فلش -->
            <% if (typeof messages !== 'undefined' && messages) { %>
                <div class="fluent-flash-messages">
                    <% if (messages.success && messages.success.length > 0) { %>
                        <div class="fluent-alert success">
                            <i class="fa-solid fa-check-circle"></i>
                            <%= messages.success[0] %>
                        </div>
                    <% } %>
                    
                    <% if (messages.error && messages.error.length > 0) { %>
                        <div class="fluent-alert error">
                            <i class="fa-solid fa-times-circle"></i>
                            <%= messages.error[0] %>
                        </div>
                    <% } %>
                    
                    <% if (messages.info && messages.info.length > 0) { %>
                        <div class="fluent-alert info">
                            <i class="fa-solid fa-info-circle"></i>
                            <%= messages.info[0] %>
                        </div>
                    <% } %>
                    
                    <% if (messages.warning && messages.warning.length > 0) { %>
                        <div class="fluent-alert warning">
                            <i class="fa-solid fa-exclamation-triangle"></i>
                            <%= messages.warning[0] %>
                        </div>
                    <% } %>
                </div>
            <% } %>

            <!-- محتوای صفحه -->
            <%- body %>
            
            <!-- پاورقی -->
            <div class="fluent-login-footer">
                <p>پنل مدیریت CCOIN &copy; <%= new Date().getFullYear() %> | <a href="https://ccoin.app" target="_blank">CCoin.app</a></p>
            </div>
        </div>
    </div>
    
    <!-- اسکریپت‌های فلوئنت UI -->
    <script src="/admin/public/js/fluent-ui.js"></script>
    
    <!-- اسکریپت‌های اضافی -->
    <% if (typeof extraScripts !== 'undefined') { %>
        <%- extraScripts %>
    <% } %>
</body>
</html>
