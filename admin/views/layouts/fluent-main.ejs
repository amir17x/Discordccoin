<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | پنل مدیریت CCOIN</title>
    
    <!-- فونت‌ها -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- آیکون‌ها -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- استایل‌های فلوئنت UI -->
    <link rel="stylesheet" href="/admin/public/css/fluent.css">
    
    <!-- چارت.جی‌اس برای نمودارها -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- اسکریپت آیکون‌های feather -->
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
    
    <!-- اسکریپت‌های اضافی -->
    <% if (typeof extraStyles !== 'undefined') { %>
        <%- extraStyles %>
    <% } %>
</head>
<body>
    <!-- بک‌دراپ منو موبایل -->
    <div class="fluent-backdrop" id="mobileMenuBackdrop"></div>

    <div class="fluent-app-container">
        <!-- نوار کناری -->
        <aside class="fluent-sidebar">
            <div class="fluent-sidebar-header">
                <div class="fluent-sidebar-logo">
                    <img src="/admin/public/img/ccoin-logo.svg" alt="CCOIN">
                    <span>CCOIN</span>
                </div>
                <button class="fluent-sidebar-close-btn" id="closeSidebarBtn">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>
            
            <div class="fluent-sidebar-content">
                <nav class="fluent-navigation">
                    <ul>
                        <li class="<%= currentRoute === '/admin/dashboard' ? 'active' : '' %>">
                            <a href="/admin/dashboard">
                                <i class="fa-solid fa-gauge-high"></i>
                                <span>داشبورد</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/users') ? 'active' : '' %>">
                            <a href="/admin/users">
                                <i class="fa-solid fa-users"></i>
                                <span>کاربران</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/economy') ? 'active' : '' %>">
                            <a href="/admin/economy">
                                <i class="fa-solid fa-money-bill-trend-up"></i>
                                <span>اقتصاد</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/servers') ? 'active' : '' %>">
                            <a href="/admin/servers">
                                <i class="fa-solid fa-server"></i>
                                <span>سرورها</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/shop') ? 'active' : '' %>">
                            <a href="/admin/shop">
                                <i class="fa-solid fa-shopping-cart"></i>
                                <span>فروشگاه</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/games') ? 'active' : '' %>">
                            <a href="/admin/games">
                                <i class="fa-solid fa-gamepad"></i>
                                <span>بازی‌ها</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/events') ? 'active' : '' %>">
                            <a href="/admin/events">
                                <i class="fa-solid fa-calendar-days"></i>
                                <span>رویدادها</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/giftcodes') ? 'active' : '' %>">
                            <a href="/admin/giftcodes">
                                <i class="fa-solid fa-gift"></i>
                                <span>کدهای هدیه</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/logs') ? 'active' : '' %>">
                            <a href="/admin/logs">
                                <i class="fa-solid fa-list"></i>
                                <span>لاگ‌ها</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/settings') ? 'active' : '' %>">
                            <a href="/admin/settings">
                                <i class="fa-solid fa-gear"></i>
                                <span>تنظیمات</span>
                            </a>
                        </li>
                        
                        <li class="<%= currentRoute && currentRoute.startsWith('/admin/admins') ? 'active' : '' %>">
                            <a href="/admin/admins">
                                <i class="fa-solid fa-user-shield"></i>
                                <span>مدیران</span>
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
            
            <div class="fluent-sidebar-footer">
                <p>CCOIN Admin Panel v1.2</p>
            </div>
        </aside>
        
        <!-- محتوای اصلی -->
        <div class="fluent-content">
            <!-- هدر -->
            <header class="fluent-header">
                <button class="fluent-mobile-menu-btn" id="mobileMenuBtn">
                    <i class="fa-solid fa-bars"></i>
                </button>
                
                <div class="fluent-header-actions">
                    <div class="fluent-search">
                        <input type="text" class="fluent-search-input" placeholder="جستجو...">
                        <i class="fa-solid fa-search fluent-search-icon"></i>
                    </div>
                    
                    <div class="fluent-user-menu">
                        <div class="fluent-user-avatar">
                            <img src="/admin/public/img/user-avatar.svg" alt="Admin">
                        </div>
                        <span class="fluent-user-name"><%= user ? user.name || user.username : 'ادمین' %></span>
                        <i class="fa-solid fa-chevron-down"></i>
                        
                        <div class="fluent-user-dropdown">
                            <ul>
                                <li>
                                    <a href="/admin/profile">
                                        <i class="fa-solid fa-user"></i>
                                        پروفایل
                                    </a>
                                </li>
                                <li>
                                    <a href="/admin/settings">
                                        <i class="fa-solid fa-gear"></i>
                                        تنظیمات
                                    </a>
                                </li>
                                <div class="fluent-divider"></div>
                                <li>
                                    <a href="/admin/logout">
                                        <i class="fa-solid fa-sign-out-alt"></i>
                                        خروج
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- پیام‌های فلش -->
            <% if (typeof messages !== 'undefined' && messages) { %>
                <div class="fluent-flash-messages">
                    <div class="fluent-content-body">
                        <% if (messages.success && messages.success.length > 0) { %>
                            <div class="fluent-alert success">
                                <i class="fa-solid fa-check-circle"></i>
                                <%= messages.success[0] %>
                            </div>
                        <% } %>
                        
                        <% if (messages.error && messages.error.length > 0) { %>
                            <div class="fluent-alert error">
                                <i class="fa-solid fa-times-circle"></i>
                                <%= messages.error[0] %>
                            </div>
                        <% } %>
                        
                        <% if (messages.info && messages.info.length > 0) { %>
                            <div class="fluent-alert info">
                                <i class="fa-solid fa-info-circle"></i>
                                <%= messages.info[0] %>
                            </div>
                        <% } %>
                        
                        <% if (messages.warning && messages.warning.length > 0) { %>
                            <div class="fluent-alert warning">
                                <i class="fa-solid fa-exclamation-triangle"></i>
                                <%= messages.warning[0] %>
                            </div>
                        <% } %>
                    </div>
                </div>
            <% } %>
            
            <!-- محتوای اصلی صفحه -->
            <%- body %>
        </div>
    </div>
    
    <!-- اسکریپت‌های فلوئنت UI -->
    <script src="/admin/public/js/fluent-ui.js"></script>
    
    <!-- اسکریپت‌های اضافی -->
    <% if (typeof extraScripts !== 'undefined') { %>
        <%- extraScripts %>
    <% } %>
</body>
</html>
