<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>فراموشی رمز عبور | پنل مدیریت CCOIN</title>
    
    <!-- فونت‌ها -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- آیکون‌ها -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- استایل‌های فلوئنت -->
    <link rel="stylesheet" href="/admin/public/css/fluent.css">
    
    <style>
        :root {
            /* رنگ‌های اصلی */
            --accent: #0078d4;
            --accent-light: #2b88d8;
            --accent-dark: #006cbe;
            
            /* رنگ‌های خنثی */
            --neutral-foreground: #242424;
            --neutral-foreground-secondary: #616161;
            --neutral-background: #ffffff;
            --neutral-background-secondary: #f5f5f5;
            --neutral-background-tertiary: #e1e1e1;
            --neutral-darker: #d1d1d1;
            --neutral-lighter: #f4f4f4;
            
            /* رنگ‌های تکمیلی */
            --success: #107c10;
            --success-light: #dff6dd;
            --warning: #d83b01;
            --warning-light: #fed9cc;
            --error: #d13438;
            --error-light: #fde7e9;
            --info: #0078d4;
            --info-light: #d0e7f8;
            
            /* رنگ‌های عناصر */
            --text-primary: var(--neutral-foreground);
            --text-secondary: var(--neutral-foreground-secondary);
            --text-tertiary: #888888;
            --surface-background: var(--neutral-background);
            --surface-background-hover: var(--neutral-lighter);
            --surface-background-selected: var(--neutral-background-tertiary);
            --divider: var(--neutral-darker);
            --input-background: var(--neutral-background);
            --card-background: var(--neutral-background);
            
            /* سایه‌ها */
            --shadow-small: 0 1.6px 3.6px 0 rgba(0, 0, 0, 0.132), 0 0.3px 0.9px 0 rgba(0, 0, 0, 0.108);
            --shadow-medium: 0 3.2px 7.2px 0 rgba(0, 0, 0, 0.132), 0 0.6px 1.8px 0 rgba(0, 0, 0, 0.108);
            --shadow-large: 0 6.4px 14.4px 0 rgba(0, 0, 0, 0.132), 0 1.2px 3.6px 0 rgba(0, 0, 0, 0.108);
            
            /* انیمیشن */
            --animation-fast: 0.1s;
            --animation-normal: 0.2s;
            --animation-slow: 0.3s;
            
            /* گرد گوشه */
            --border-radius-small: 2px;
            --border-radius-medium: 4px;
            --border-radius-large: 8px;
            
            /* فاصله‌گذاری */
            --spacing-xs: 4px;
            --spacing-s: 8px;
            --spacing-m: 12px;
            --spacing-l: 16px;
            --spacing-xl: 20px;
            --spacing-xxl: 24px;
        }
    </style>
</head>
<body class="fluent-login-page">
    <main class="fluent-login-wrapper">
        <!-- پیام‌های فلش -->
        <% if (typeof messages !== "undefined") { %>
            <div class="fluent-flash-messages">
                <% if (messages.success && messages.success.length > 0) { %>
                    <div class="fluent-alert success">
                        <i class="fas fa-check-circle"></i>
                        <span><%= messages.success %></span>
                    </div>
                <% } %>
                
                <% if (messages.error && messages.error.length > 0) { %>
                    <div class="fluent-alert error">
                        <i class="fas fa-exclamation-triangle"></i>
                        <span><%= messages.error %></span>
                    </div>
                <% } %>
                
                <% if (messages.info && messages.info.length > 0) { %>
                    <div class="fluent-alert info">
                        <i class="fas fa-info-circle"></i>
                        <span><%= messages.info %></span>
                    </div>
                <% } %>
                
                <% if (messages.warning && messages.warning.length > 0) { %>
                    <div class="fluent-alert warning">
                        <i class="fas fa-exclamation-circle"></i>
                        <span><%= messages.warning %></span>
                    </div>
                <% } %>
            </div>
        <% } %>
        
        <div class="fluent-login-card">
            <div class="fluent-login-header">
                <img src="/admin/public/images/ccoin-logo.png" alt="CCOIN Logo">
                <h1 class="fluent-login-title">فراموشی رمز عبور</h1>
                <p class="fluent-login-subtitle">لطفا ایمیل خود را وارد کنید تا لینک بازنشانی برای شما ارسال شود</p>
            </div>
            
            <form action="/admin/forgot-password" method="POST">
                <div class="fluent-form-group">
                    <label class="fluent-label" for="email">ایمیل</label>
                    <input type="email" id="email" name="email" class="fluent-input" placeholder="ایمیل خود را وارد کنید" required autofocus>
                </div>
                
                <button type="submit" class="fluent-button fluent-button-primary fluent-button-block">
                    <i class="fas fa-paper-plane ml-2"></i>
                    ارسال لینک بازنشانی
                </button>
            </form>
            
            <div class="fluent-login-links">
                <a href="/admin/login">بازگشت به صفحه ورود</a>
            </div>
        </div>

        <div class="fluent-login-footer">
            <p>پنل مدیریت CCOIN &copy; <%= new Date().getFullYear() %></p>
            <p>طراحی شده با <i class="fas fa-heart" style="color: #d13438;"></i> و Microsoft Fluent UI</p>
        </div>
    </main>
</body>
</html>
